# Python
__pycache__/
*.py[cod]
.venv/
venv/

# PyInstaller output (GestureHud.exe is ~20MB; ship it as a release asset)
build/
dist/

# Regenerated from icon.py on every build (GestureHud.spec) - same class of
# build artifact as build/ and dist/ above, just not inside either directory.
icon.ico

# Local runtime state lives in %LOCALAPPDATA%\GestureHud, never here
*.log
settings.json
calibration.json
